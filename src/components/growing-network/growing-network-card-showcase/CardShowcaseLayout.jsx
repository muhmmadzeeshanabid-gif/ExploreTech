import React from "react";

const CardShowcaseLayout = ({ children }) => (
  <section className="w-full bg-white pb-16 pt-2">
    <div className="mx-auto w-full px-0">{children}</div>
  </section>
);

export default CardShowcaseLayout;
